# Dashboard
Dashboard des actions des decompanies canadiennes

Paste the code in VS code
Copy path of code(left click on it)
Open terminal
Type "cd" and paste path but delete the file name at the end 
Paste "streamlit run analyse.py"
MUST HAVE INSTALLED THE FOLLOWING LIBRARIES : STREAMLIT INSTALLED AS WELL AS YFINANCE MATPLOTLIB PANDAS
if not, type "pip install streamlit yfinance matplotlib pandas" in your terminal
